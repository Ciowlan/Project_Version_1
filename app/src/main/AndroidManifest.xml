<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recordingapp">

    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecordingApp">
        <activity
            android:name="com.example.recordingapp.MainActivity"
            android:exported="true">
        </activity>
        <activity
            android:name="com.example.recordingapp.Record"
            android:exported="true">
        </activity>
        <activity
            android:name="com.example.recordingapp.Start"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.recordingapp.Choose"
            android:exported="true">
        </activity>
        <activity
            android:name="com.example.recordingapp.End"
            android:exported="true">
        </activity>

    </application>

</manifest>